/* AUTO-GENERATED by build/parse-guide.js — do not edit by hand.
   Edit input/guide_en.MD and re-run: node build/parse-guide.js */
window.CCAF_THEORY = [
  {
    "id": "t1",
    "part": "Part I — Theory Foundations",
    "title": "PART I: THEORY FOUNDATIONS",
    "html": "<p>This part covers all the theory you need to pass the exam successfully. The material is organized by technologies and concepts rather than by exam domains—this helps you build a deeper understanding of each topic.</p>"
  },
  {
    "id": "t2",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 1: Claude API — Fundamentals of Model Interaction",
    "html": "<blockquote>Documentation: <a href=\"https://platform.claude.com/docs/en/api/messages\" target=\"_blank\" rel=\"noopener\">Messages API</a> | <a href=\"https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/overview\" target=\"_blank\" rel=\"noopener\">Prompt Engineering</a></blockquote>\n<h3>1.1 API Request Structure</h3>\n<p>The Claude API follows a request–response model. Each request to the Claude Messages API includes:</p>\n<pre><code class=\"lang-json\">{\n  \"model\": \"claude-sonnet-4-6\",\n  \"max_tokens\": 1024,\n  \"system\": \"You are a helpful assistant.\",\n  \"messages\": [\n    {\"role\": \"user\", \"content\": \"Hi!\"},\n    {\"role\": \"assistant\", \"content\": \"Hello!\"},\n    {\"role\": \"user\", \"content\": \"How are you?\"}\n  ],\n  \"tools\": [...],\n  \"tool_choice\": {\"type\": \"auto\"}\n}</code></pre>\n<p><strong>Key fields:</strong></p>\n<ul><li><code>model</code> — model selection (<code>claude-opus-4-6</code>, <code>claude-sonnet-4-6</code>, <code>claude-haiku-4-5</code>)</li><li><code>max_tokens</code> — maximum number of tokens in the response</li><li><code>system</code> — the system prompt (defines model behavior)</li><li><code>messages</code> — conversation history (<strong>you must send the full history</strong> to maintain coherence)</li><li><code>tools</code> — definitions of available tools</li><li><code>tool_choice</code> — tool selection strategy</li></ul>\n<h3>1.2 Message Roles</h3>\n<p>The <code>messages</code> array uses three roles:</p>\n<ul><li><code>user</code> — user messages</li><li><code>assistant</code> — model responses (included when sending history)</li><li><code>tool</code> — tool call results (the role is not explicitly set; this appears as a <code>tool_result</code> content block)</li></ul>\n<p><strong>Critically important:</strong> on every API request you must send the <strong>full conversation history</strong>. The model does not persist state between requests—each call is independent.</p>\n<h3>1.3 The <code>stop_reason</code> Field in the Response</h3>\n<p>The Claude API response includes <code>stop_reason</code>, which indicates why the model stopped generating:</p>\n<div class=\"table-wrap\"><table><thead><tr><th>Value</th><th>Description</th><th>Action</th></tr></thead><tbody><tr><td><code>\"end_turn\"</code></td><td>The model finished its response</td><td>Show the result to the user</td></tr><tr><td><code>\"tool_use\"</code></td><td>The model wants to call a tool</td><td>Execute the tool and return the result</td></tr><tr><td><code>\"max_tokens\"</code></td><td>Token limit reached</td><td>The response is truncated; you may need to increase the limit</td></tr><tr><td><code>\"stop_sequence\"</code></td><td>A stop sequence was encountered</td><td>Handle based on your application logic</td></tr></tbody></table></div>\n<p>For agentic systems, <code>\"tool_use\"</code> and <code>\"end_turn\"</code> are the most important—they control the agent loop.</p>\n<h3>1.4 System Prompt</h3>\n<p>The system prompt is a special instruction that defines context and behavioral rules. It:</p>\n<ul><li>Is not part of the <code>messages</code> array; it is passed separately in the <code>system</code> field</li><li>Has priority over user messages</li><li>Is loaded once and applies throughout the conversation</li><li>Is used to define role, constraints, and output format</li></ul>\n<p><strong>Important for the exam:</strong> system prompt wording can create unintended tool associations. For example, an instruction like “always verify the customer” can cause the model to overuse <code>get_customer</code>, even when it is unnecessary.</p>\n<h3>1.5 Context Window</h3>\n<p>The context window is the total amount of text (in tokens) the model can process at once. It includes:</p>\n<ul><li>The system prompt</li><li>The full message history</li><li>Tool definitions</li><li>Tool results</li></ul>\n<p><strong>Key context-window problems:</strong></p>\n<ol><li><strong>Lost-in-the-middle effect:</strong> models reliably process information at the start and end of a long input but can miss details in the middle. Mitigation: place key information near the beginning or end.</li></ol>\n<ol><li><strong>Accumulation of tool results:</strong> every tool call adds output to the context. If a tool returns 40+ fields but only 5 matter, then most of the context is wasted.</li></ol>\n<ol><li><strong>Progressive summarization:</strong> when compressing history, numeric values, percentages, and dates often get lost and become vague (“about”, “roughly”, “a few”).</li></ol>"
  },
  {
    "id": "t3",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 2: Tools and `tool_use`",
    "html": "<blockquote>Documentation: <a href=\"https://platform.claude.com/docs/en/build-with-claude/tool-use\" target=\"_blank\" rel=\"noopener\">Tool Use</a></blockquote>\n<h3>2.1 What is <code>tool_use</code></h3>\n<p><code>tool_use</code> is a mechanism that allows Claude to call external functions. The model does not run code directly—it generates a structured tool call request; your code executes it and returns the result.</p>\n<h3>2.2 Tool Definition</h3>\n<p>Each tool is defined using a JSON schema:</p>\n<pre><code class=\"lang-json\">{\n  \"name\": \"get_customer\",\n  \"description\": \"Finds a customer by email or ID. Returns the customer profile, including name, email, order history, and account status. Use this tool BEFORE lookup_order to verify the customer's identity. Accepts an email (format: user@domain.com) or a numeric customer_id.\",\n  \"input_schema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"email\": {\"type\": \"string\", \"description\": \"Customer email\"},\n      \"customer_id\": {\"type\": \"integer\", \"description\": \"Numeric customer ID\"}\n    },\n    \"required\": []\n  }\n}</code></pre>\n<p><strong>Critically important aspects of a tool description:</strong></p>\n<ol><li><strong>The description is the primary selection mechanism.</strong> An LLM chooses tools based on their descriptions. Minimal descriptions (“Retrieves customer information”) lead to mistakes when tools overlap.</li></ol>\n<ol><li><strong>Include in the description:</strong></li></ol>\n<ul><li>What the tool does and returns</li><li>Input formats and example values</li><li>Edge cases and constraints</li><li>When to use this tool vs similar alternatives</li></ul>\n<ol><li><strong>Avoid</strong> identical or overlapping descriptions across tools. If <code>analyze_content</code> and <code>analyze_document</code> have nearly identical descriptions, the model will confuse them.</li></ol>\n<ol><li><strong>Built-in tools vs MCP tools:</strong> agents may prefer built-in tools (Read, Grep) over MCP tools with similar functionality. To prevent this, strengthen MCP tool descriptions—highlight concrete advantages, unique data, or context that built-in tools cannot provide.</li></ol>\n<h3>2.3 The <code>tool_choice</code> Parameter</h3>\n<p><code>tool_choice</code> controls how the model selects tools:</p>\n<div class=\"table-wrap\"><table><thead><tr><th>Value</th><th>Behavior</th><th>When to use</th></tr></thead><tbody><tr><td><code>{\"type\": \"auto\"}</code></td><td>The model decides whether to call a tool or answer in text</td><td>Default for most cases</td></tr><tr><td><code>{\"type\": \"any\"}</code></td><td>The model <strong>must</strong> call some tool</td><td>When you need guaranteed structured output</td></tr><tr><td><code>{\"type\": \"tool\", \"name\": \"extract_metadata\"}</code></td><td>The model <strong>must</strong> call a specific tool</td><td>When you need a forced first step / execution order</td></tr></tbody></table></div>\n<p><strong>Important scenarios:</strong></p>\n<ul><li><code>tool_choice: \"any\"</code> + multiple extraction tools → the model picks the best one, but you still get structured output</li><li>Forced selection → when you must guarantee a specific first action (e.g., <code>extract_metadata</code> before enrichment)</li></ul>\n<h3>2.4 JSON Schemas for Structured Output</h3>\n<p>Using <code>tool_use</code> with JSON schemas is the <strong>most reliable</strong> way to obtain structured output from Claude. It:</p>\n<ul><li>Guarantees syntactically valid JSON (no missing braces, no trailing commas)</li><li>Enforces the required structure (required fields are present)</li><li>Does <strong>not</strong> guarantee semantic correctness (values can still be wrong)</li></ul>\n<p><strong>Schema design — key principles:</strong></p>\n<pre><code class=\"lang-json\">{\n  \"type\": \"object\",\n  \"properties\": {\n    \"category\": {\n      \"type\": \"string\",\n      \"enum\": [\"bug\", \"feature\", \"docs\", \"unclear\", \"other\"]\n    },\n    \"category_detail\": {\n      \"type\": [\"string\", \"null\"],\n      \"description\": \"Details if category = 'other' or 'unclear'\"\n    },\n    \"severity\": {\n      \"type\": \"string\",\n      \"enum\": [\"critical\", \"high\", \"medium\", \"low\"]\n    },\n    \"confidence\": {\n      \"type\": \"number\",\n      \"minimum\": 0,\n      \"maximum\": 1\n    },\n    \"optional_field\": {\n      \"type\": [\"string\", \"null\"],\n      \"description\": \"Null if the information was not found in the source\"\n    }\n  },\n  \"required\": [\"category\", \"severity\"]\n}</code></pre>\n<p><strong>Schema design rules:</strong></p>\n<ol><li><strong>Required vs optional:</strong> mark fields as required only if the information is always available. Required fields push the model to fabricate values when data is missing.</li><li><strong>Nullable fields:</strong> use <code>\"type\": [\"string\", \"null\"]</code> for information that may be absent. The model can return <code>null</code> instead of hallucinating.</li><li><strong>Enums with <code>\"other\"</code>:</strong> add <code>\"other\"</code> + a detail string to avoid losing data outside your predefined categories.</li><li><strong>Enum <code>\"unclear\"</code>:</strong> for cases where the model cannot confidently pick a category—honest <code>\"unclear\"</code> is better than a wrong category.</li></ol>\n<h3>2.5 Syntax vs Semantic Errors</h3>\n<div class=\"table-wrap\"><table><thead><tr><th>Error type</th><th>Example</th><th>Mitigation</th></tr></thead><tbody><tr><td><strong>Syntax</strong></td><td>Invalid JSON, wrong field type</td><td><code>tool_use</code> with a JSON schema (eliminates)</td></tr><tr><td><strong>Semantic</strong></td><td>Totals don't add up, value in wrong field, hallucination</td><td>Validation checks, retry with feedback, self-correction</td></tr></tbody></table></div>"
  },
  {
    "id": "t4",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 3: Claude Agent SDK — Building Agentic Systems",
    "html": "<blockquote>Documentation: <a href=\"https://platform.claude.com/docs/en/agent-sdk/overview\" target=\"_blank\" rel=\"noopener\">Agent SDK</a> | <a href=\"https://platform.claude.com/docs/en/agent-sdk/hooks\" target=\"_blank\" rel=\"noopener\">Hooks</a> | <a href=\"https://platform.claude.com/docs/en/agent-sdk/subagents\" target=\"_blank\" rel=\"noopener\">Subagents</a> | <a href=\"https://platform.claude.com/docs/en/agent-sdk/sessions\" target=\"_blank\" rel=\"noopener\">Sessions</a></blockquote>\n<h3>3.1 What is an Agentic Loop</h3>\n<p>The agentic loop is the core pattern for autonomous task execution. The model doesn't just answer—it performs a sequence of actions:</p>\n<pre><code>1. Send a request to Claude with tools\n2. Receive a response\n3. Check stop_reason:\n   - \"tool_use\" -&gt; execute the tool, append the result to history, go back to step 1\n   - \"end_turn\" -&gt; the task is complete, show the result to the user\n4. Repeat until completion</code></pre>\n<p><strong>This is a model-driven approach:</strong> Claude decides which tool to call next based on context and prior tool results. This differs from hard-coded decision trees where the action sequence is fixed.</p>\n<p><strong>Anti-patterns (avoid):</strong></p>\n<ul><li>Parsing assistant text to detect completion (“Task completed”)</li><li>Using an arbitrary iteration limit (e.g., <code>max_iterations=5</code>) as the primary stop condition</li><li>Checking whether the assistant produced textual content as a completion signal</li></ul>\n<p><strong>Correct approach:</strong> the only reliable completion signal is <code>stop_reason == \"end_turn\"</code>.</p>\n<h3>3.2 <code>AgentDefinition</code> Configuration</h3>\n<p><code>AgentDefinition</code> is the agent configuration object in the Claude Agent SDK:</p>\n<pre><code class=\"lang-python\">agent = AgentDefinition(\n    name=\"customer_support\",\n    description=\"Handles customer requests for returns and order issues\",\n    system_prompt=\"You are a customer support agent...\",\n    allowed_tools=[\"get_customer\", \"lookup_order\", \"process_refund\", \"escalate_to_human\"],\n    # For a coordinator:\n    # allowed_tools=[\"Task\", \"get_customer\", ...]\n)</code></pre>\n<p><strong>Key parameters:</strong></p>\n<ul><li><code>name</code> / <code>description</code> — identification and description of the agent</li><li><code>system_prompt</code> — system prompt with instructions</li><li><code>allowed_tools</code> — list of allowed tools (principle of least privilege)</li></ul>\n<h3>3.3 Hub-and-Spoke: Coordinator and Subagents</h3>\n<p>A multi-agent architecture is typically built as a hub-and-spoke topology:</p>\n<pre><code>         Coordinator\n        /     |      \\\n   Subagent1  Subagent2  Subagent3\n    (search)   (analysis)   (synthesis)</code></pre>\n<p><strong>The coordinator is responsible for:</strong></p>\n<ul><li>Decomposing the task into subtasks</li><li>Deciding which subagents are needed (dynamic selection)</li><li>Delegating work to subagents</li><li>Aggregating and validating results</li><li>Handling errors and retries</li><li>Communicating results to the user</li></ul>\n<p><strong>Critical principle: subagents have isolated context.</strong></p>\n<ul><li>Subagents do <strong>not</strong> automatically inherit the coordinator's conversation history</li><li>All required context must be <strong>explicitly passed</strong> in the subagent prompt</li><li>Subagents do not share memory across calls</li><li>All communication flows through the coordinator (for observability and error control)</li></ul>\n<h3>3.4 The <code>Task</code> Tool for Spawning Subagents</h3>\n<p>Subagents are spawned via the <code>Task</code> tool:</p>\n<pre><code class=\"lang-python\"># The coordinator's allowedTools must include \"Task\"\ncoordinator_agent = AgentDefinition(\n    allowed_tools=[\"Task\", \"get_customer\"]\n)</code></pre>\n<p><strong>Explicit context passing is mandatory:</strong></p>\n<pre><code># Bad: the subagent has no context\nTask: \"Analyze the document\"\n\n# Good: full context in the prompt\nTask: \"Analyze the following document.\nDocument: [full document text]\nPrior search results: [web search results]\nOutput format requirements: [schema]\"</code></pre>\n<p><strong>Parallel spawning:</strong> a coordinator can call multiple <code>Task</code>s in one response—subagents run in parallel:</p>\n<pre><code># One coordinator response contains:\nTask 1: \"Search for articles about X\"\nTask 2: \"Analyze document Y\"\nTask 3: \"Search for articles about Z\"\n# All three run concurrently</code></pre>\n<h3>3.5 Hooks in the Agent SDK</h3>\n<p>Hooks allow interception and transformation at specific points in the agent lifecycle.</p>\n<p><strong>PostToolUse</strong> intercepts a tool result before it is provided to the model:</p>\n<pre><code class=\"lang-python\"># Example: normalize date formats from different MCP tools\n@hook(\"PostToolUse\")\ndef normalize_dates(tool_result):\n    # Convert Unix timestamp -&gt; ISO 8601\n    # Convert \"Mar 5, 2025\" -&gt; \"2025-03-05\"\n    return normalized_result</code></pre>\n<p><strong>Outgoing-call interception hook</strong> blocks actions that violate policy:</p>\n<pre><code class=\"lang-python\"># Example: block refunds above $500\n@hook(\"PreToolUse\")\ndef enforce_refund_limit(tool_call):\n    if tool_call.name == \"process_refund\" and tool_call.args.amount &gt; 500:\n        return redirect_to_escalation(tool_call)</code></pre>\n<p><strong>Key difference: hooks vs prompt instructions</strong></p>\n<div class=\"table-wrap\"><table><thead><tr><th>Attribute</th><th>Hooks</th><th>Prompt instructions</th></tr></thead><tbody><tr><td>Guarantee</td><td><strong>Deterministic</strong> (100%)</td><td><strong>Probabilistic</strong> (&gt;90%, not 100%)</td></tr><tr><td>When to use</td><td>Critical business rules, financial operations, compliance</td><td>General preferences, recommendations, formatting</td></tr><tr><td>Example</td><td>Block refunds &gt; $500</td><td>“Try to solve before escalating”</td></tr></tbody></table></div>\n<p><strong>Rule:</strong> when failure has financial, legal, or safety consequences—use hooks, not prompts.</p>"
  },
  {
    "id": "t5",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 4: Model Context Protocol (MCP)",
    "html": "<blockquote>Documentation: <a href=\"https://modelcontextprotocol.io/\" target=\"_blank\" rel=\"noopener\">MCP</a> | <a href=\"https://modelcontextprotocol.io/docs/concepts/tools\" target=\"_blank\" rel=\"noopener\">Tools</a> | <a href=\"https://modelcontextprotocol.io/docs/concepts/resources\" target=\"_blank\" rel=\"noopener\">Resources</a> | <a href=\"https://modelcontextprotocol.io/docs/concepts/servers\" target=\"_blank\" rel=\"noopener\">Servers</a></blockquote>\n<h3>4.1 What is MCP</h3>\n<p>The Model Context Protocol (MCP) is an open protocol for connecting external systems to Claude. MCP defines three primary resource types:</p>\n<ol><li><strong>Tools</strong> — functions the agent can call to perform actions (CRUD operations, API calls, command execution)</li><li><strong>Resources</strong> — data the agent can read for context (documentation, database schemas, content catalogs)</li><li><strong>Prompts</strong> — predefined prompt templates for common tasks</li></ol>\n<h3>4.2 MCP Servers</h3>\n<p>An MCP server is a process that implements the MCP protocol and provides tools/resources. When you connect to an MCP server:</p>\n<ul><li>All tools are discovered automatically</li><li>Tools from all connected servers are available at once</li><li>Tool descriptions determine how the model will use them</li></ul>\n<h3>4.3 Configuring MCP Servers</h3>\n<p><strong>Project configuration (<code>.mcp.json</code>)</strong> — for team usage:</p>\n<pre><code class=\"lang-json\">{\n  \"mcpServers\": {\n    \"github\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"@modelcontextprotocol/server-github\"],\n      \"env\": {\n        \"GITHUB_TOKEN\": \"${GITHUB_TOKEN}\"\n      }\n    },\n    \"jira\": {\n      \"command\": \"npx\",\n      \"args\": [\"-y\", \"mcp-server-jira\"],\n      \"env\": {\n        \"JIRA_TOKEN\": \"${JIRA_TOKEN}\"\n      }\n    }\n  }\n}</code></pre>\n<p><strong>Key points:</strong></p>\n<ul><li><code>.mcp.json</code> is stored at the project root and managed in version control</li><li>Environment variables (<code>${GITHUB_TOKEN}</code>) are used for secrets—tokens themselves are not committed</li><li>Available to all project contributors</li></ul>\n<p><strong>User configuration (<code>~/.claude.json</code>)</strong> — for personal/experimental servers:</p>\n<ul><li>Stored in the user's home directory</li><li>Not shared via version control</li><li>Suitable for personal experiments and testing</li></ul>\n<p><strong>Choosing servers:</strong></p>\n<ul><li>For standard integrations (Jira, GitHub, Slack), prefer existing community MCP servers</li><li>Build your own servers only for unique, team-specific workflows</li></ul>\n<h3>4.4 The <code>isError</code> Flag in MCP</h3>\n<p>When an MCP tool encounters an error, it uses <code>isError: true</code> in the response. This signals to the agent that the call failed.</p>\n<p><strong>Structured error (good):</strong></p>\n<pre><code class=\"lang-json\">{\n  \"isError\": true,\n  \"content\": {\n    \"errorCategory\": \"transient\",\n    \"isRetryable\": true,\n    \"message\": \"The service is temporarily unavailable. Timeout while calling the orders API.\",\n    \"attempted_query\": \"order_id=12345\",\n    \"partial_results\": null\n  }\n}</code></pre>\n<p><strong>Generic error (anti-pattern):</strong></p>\n<pre><code class=\"lang-json\">{\n  \"isError\": true,\n  \"content\": \"Operation failed\"\n}</code></pre>\n<p>A generic error gives the agent no information for decision-making—should it retry, change the query, or escalate?</p>\n<h3>4.5 MCP Resources</h3>\n<p>Resources are data that an agent can request to get context without taking actions:</p>\n<ul><li>Content catalogs (e.g., a list of all project tasks, hierarchical navigation)</li><li>Database schemas (understanding data structure)</li><li>Documentation (API references, internal guides)</li><li>Issue/task summaries</li></ul>\n<p><strong>Resource advantage:</strong> the agent does not need exploratory tool calls to understand what data exists. A resource provides an immediate “map.”</p>"
  },
  {
    "id": "t6",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 5: Claude Code — Configuration and Workflows",
    "html": "<blockquote>Documentation: <a href=\"https://code.claude.com/docs/en/overview\" target=\"_blank\" rel=\"noopener\">Claude Code</a> | <a href=\"https://code.claude.com/docs/en/memory\" target=\"_blank\" rel=\"noopener\">Memory / CLAUDE.md</a> | <a href=\"https://code.claude.com/docs/en/skills\" target=\"_blank\" rel=\"noopener\">Skills</a> | <a href=\"https://code.claude.com/docs/en/mcp\" target=\"_blank\" rel=\"noopener\">MCP</a> | <a href=\"https://code.claude.com/docs/en/hooks\" target=\"_blank\" rel=\"noopener\">Hooks</a> | <a href=\"https://code.claude.com/docs/en/sub-agents\" target=\"_blank\" rel=\"noopener\">Sub-agents</a> | <a href=\"https://code.claude.com/docs/en/github-actions\" target=\"_blank\" rel=\"noopener\">GitHub Actions</a> | <a href=\"https://code.claude.com/docs/en/headless\" target=\"_blank\" rel=\"noopener\">Headless</a></blockquote>\n<h3>5.1 The CLAUDE.md Hierarchy</h3>\n<p>CLAUDE.md is the instruction file(s) for Claude Code. There is a three-level hierarchy:</p>\n<pre><code>1. User-level: ~/.claude/CLAUDE.md\n   - Applies only to that user\n   - NOT shared via VCS\n   - Personal preferences and working style\n\n2. Project-level: .claude/CLAUDE.md or a root CLAUDE.md\n   - Applies to all project contributors\n   - Managed via VCS\n   - Coding standards, testing standards, architectural decisions\n\n3. Directory-level: CLAUDE.md in subdirectories\n   - Applies when working with files in that directory\n   - Conventions specific to that part of the codebase</code></pre>\n<p><strong>Common mistake:</strong> a new team member does not receive project instructions because they were placed in <code>~/.claude/CLAUDE.md</code> (user-level) instead of <code>.claude/CLAUDE.md</code> (project-level).</p>\n<h3>5.2 <code>@path</code> Syntax (File Imports)</h3>\n<p>CLAUDE.md can reference external files using <code>@path</code>, making configuration modular:</p>\n<pre><code class=\"lang-markdown\"># Project CLAUDE.md\n\nCoding standards are described in @./standards/coding-style.md\nTest requirements are in @./standards/testing-requirements.md\nProject overview is in @README.md and dependencies are in @package.json</code></pre>\n<p><strong>Rules for <code>@path</code>:</strong></p>\n<ul><li>Use <code>@</code> immediately before the file path (no space)</li><li>Relative and absolute paths are supported</li><li>Relative paths are resolved relative to the file that contains the import</li><li>Maximum import nesting depth is 5</li></ul>\n<p>This avoids duplication and lets each package include only relevant standards.</p>\n<h3>5.3 The <code>.claude/rules/</code> Directory</h3>\n<p><code>.claude/rules/</code> is an alternative to a monolithic CLAUDE.md, used to organize rules by topic:</p>\n<pre><code>.claude/rules/\n  testing.md          -- testing conventions\n  api-conventions.md  -- API conventions\n  deployment.md       -- deployment rules\n  react-patterns.md   -- React patterns</code></pre>\n<p><strong>Key feature: YAML frontmatter with <code>paths</code> for conditional loading:</strong></p>\n<pre><code class=\"lang-yaml\">---\npaths: [\"src/api/**/*\"]\n---\n\nFor API files, use async/await with explicit error handling.\nEach endpoint must return a standard response wrapper.</code></pre>\n<pre><code class=\"lang-yaml\">---\npaths: [\"**/*.test.tsx\", \"**/*.test.ts\"]\n---\n\nTests must use describe/it blocks.\nUse data factories instead of hardcoding.\nDo not mock the database—use a test database.</code></pre>\n<p><strong>How it works:</strong></p>\n<ul><li>A rule is loaded <strong>only</strong> when Claude Code edits a file matching the <code>paths</code> pattern</li><li>This saves context and tokens—irrelevant rules are not loaded</li><li>Glob patterns let you apply conventions by file type regardless of location (ideal for tests scattered across the codebase)</li></ul>\n<p><strong>When to use <code>.claude/rules/</code> with <code>paths</code> vs directory-level CLAUDE.md:</strong></p>\n<ul><li><code>.claude/rules/</code> with <code>paths</code> — when conventions apply to files spread across many directories (tests, migrations)</li><li>Directory-level CLAUDE.md — when conventions are tied to a specific directory and are not needed elsewhere</li></ul>\n<h3>5.4 Custom Slash Commands and Skills</h3>\n<blockquote><strong>Note:</strong> in the current Claude Code version, custom commands (<code>.claude/commands/</code>) are unified with skills (<code>.claude/skills/</code>). Both formats create <code>/name</code> commands. The exam guide references <code>.claude/commands/</code>—that format is still supported.</blockquote>\n<p>Slash commands are reusable prompt templates invoked via <code>/name</code>:</p>\n<p><strong><code>.claude/commands/</code> format (legacy, supported):</strong></p>\n<pre><code>.claude/commands/\n  review.md        -- /review -- standard code review\n  test-gen.md      -- /test-gen -- test generation</code></pre>\n<p><strong><code>.claude/skills/</code> format (current):</strong></p>\n<pre><code>.claude/skills/\n  review/SKILL.md  -- /review -- with frontmatter configuration\n  test-gen/SKILL.md</code></pre>\n<p><strong>Project commands</strong> (<code>.claude/commands/</code> or <code>.claude/skills/</code>):</p>\n<ul><li>Stored in VCS and available to everyone when cloning the repo</li><li>Ensure consistent workflows across the team</li></ul>\n<p><strong>User commands</strong> (<code>~/.claude/commands/</code> or <code>~/.claude/skills/</code>):</p>\n<ul><li>Personal commands not shared via VCS</li><li>For individual workflows</li></ul>\n<h3>5.5 Skills — <code>.claude/skills/</code></h3>\n<p>Skills are advanced commands configured via SKILL.md frontmatter:</p>\n<pre><code class=\"lang-yaml\">---\ncontext: fork\nallowed-tools: [\"Read\", \"Grep\", \"Glob\"]\nargument-hint: \"Path to the directory to analyze\"\n---\n\nAnalyze the code structure in the specified directory.\nOutput a report on dependencies and architectural patterns.</code></pre>\n<p><strong>Frontmatter parameters:</strong></p>\n<div class=\"table-wrap\"><table><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>context: fork</code></td><td>Runs the skill in an isolated subagent. Verbose output does not pollute the main session</td></tr><tr><td><code>allowed-tools</code></td><td>Restricts which tools are available (security—e.g., the skill cannot delete files if not allowed)</td></tr><tr><td><code>argument-hint</code></td><td>Hint that asks for an argument when invoked without parameters</td></tr></tbody></table></div>\n<p><strong>When to use a skill vs CLAUDE.md:</strong></p>\n<ul><li><strong>Skill</strong> — on-demand invocation for a specific task (review, analysis, generation)</li><li><strong>CLAUDE.md</strong> — always-loaded general standards and conventions</li></ul>\n<p><strong>Personal skills (<code>~/.claude/skills/</code>):</strong></p>\n<ul><li>Create personal variants under different names so you don't affect teammates</li></ul>\n<h3>5.6 Planning Mode vs Direct Execution</h3>\n<p><strong>Planning mode:</strong></p>\n<ul><li>The model only investigates and plans; it does not make changes</li><li>Uses Read, Grep, Glob to explore the codebase</li><li>Produces an implementation plan that the user approves</li><li>Safe exploration with no side effects</li></ul>\n<p><strong>When to use planning mode:</strong></p>\n<ul><li>Large changes (dozens of files)</li><li>Multiple plausible approaches (microservices: how to define boundaries?)</li><li>Architectural decisions (which framework? what structure?)</li><li>Unfamiliar codebase (you must understand before changing)</li><li>Library migrations affecting 45+ files</li></ul>\n<p><strong>When to use direct execution:</strong></p>\n<ul><li>Single-file fixes with a clear stack trace</li><li>Adding one validation check</li><li>Well-understood, unambiguous changes</li></ul>\n<p><strong>Combined approach:</strong></p>\n<ol><li>Planning mode for investigation and design</li><li>User approves the plan</li><li>Direct execution to implement the approved plan</li></ol>\n<p><strong>Explore subagent</strong> — a specialized subagent for exploring the codebase:</p>\n<ul><li>Isolates verbose output from the main context</li><li>Returns only a summary</li><li>Prevents context-window exhaustion in multi-phase tasks</li></ul>\n<h3>5.7 The <code>/compact</code> Command</h3>\n<p><code>/compact</code> is a built-in command for compressing context:</p>\n<ul><li>Summarizes prior history to free up the context window</li><li>Used in long investigation sessions when the context fills up with verbose tool output</li><li>Risk: exact numeric values, dates, and specific details can be lost during summarization</li></ul>\n<h3>5.8 The <code>/memory</code> Command</h3>\n<p><code>/memory</code> is a built-in command for managing memory between sessions:</p>\n<ul><li>Opens the <code>CLAUDE.md</code> file for editing, allowing you to save notes, preferences, and context</li><li>Information persists across sessions and is automatically loaded on startup</li><li>Useful for storing project conventions, user preferences, frequently used commands, and current work context</li><li>Alternative to re-explaining the same instructions in every session</li></ul>\n<h3>5.9 Claude Code CLI for CI/CD</h3>\n<p><strong>The <code>-p</code> (or <code>--print</code>) flag:</strong></p>\n<pre><code class=\"lang-bash\">claude -p \"Analyze this pull request for security issues\"</code></pre>\n<ul><li>Non-interactive mode: processes the prompt, prints to stdout, exits</li><li>Does not wait for user input</li><li>The only correct way to run Claude in CI/CD pipelines</li></ul>\n<p><strong>Structured output for CI:</strong></p>\n<pre><code class=\"lang-bash\">claude -p \"Review this PR\" --output-format json --json-schema '{\"type\":\"object\",...}'</code></pre>\n<ul><li><code>--output-format json</code> — output in JSON</li><li><code>--json-schema</code> — validate output against a schema</li><li>The result can be parsed to automatically post inline PR comments</li></ul>\n<p><strong>Session context isolation:</strong> The same Claude session that generated code is often less effective at reviewing it (the model retains its reasoning context and is less likely to challenge its own decisions). Use an independent instance for review.</p>\n<p><strong>Preventing duplicate comments:</strong> When re-reviewing after new commits, include prior review results in context and instruct Claude to report only new or unresolved issues.</p>\n<h3>5.10 <code>fork_session</code> and Session Management</h3>\n<p><strong><code>--resume &lt;session-name&gt;</code></strong> resumes a named session:</p>\n<pre><code class=\"lang-bash\">claude --resume investigation-auth-bug</code></pre>\n<ul><li>Continues a prior conversation with saved context</li><li>Useful for long investigations across multiple sessions</li><li>Risk: if files changed since the prior session, tool results may be stale</li></ul>\n<p><strong><code>fork_session</code></strong> creates an independent branch from shared context:</p>\n<pre><code>Codebase investigation\n         |\n    fork_session\n    /           \\\nApproach A:      Approach B:\nRedux            Context API</code></pre>\n<ul><li>Both forks inherit context up to the branch point</li><li>Afterwards, they diverge independently</li><li>Useful for comparing approaches or testing strategies</li></ul>\n<p><strong>When to start a new session instead of resuming:</strong></p>\n<ul><li>Tool results are stale (files changed)</li><li>A lot of time has passed and context has degraded</li><li>It is better to restart with \"Here is a short summary of what we found: ...\" than to resume with old tool data</li></ul>"
  },
  {
    "id": "t7",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 6: Prompt Engineering — Advanced Techniques",
    "html": "<blockquote>Documentation: <a href=\"https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/overview\" target=\"_blank\" rel=\"noopener\">Prompt Engineering</a> | <a href=\"https://github.com/anthropics/anthropic-cookbook\" target=\"_blank\" rel=\"noopener\">Anthropic Cookbook</a></blockquote>\n<h3>6.1 Few-shot Prompting</h3>\n<p>Few-shot prompting is the inclusion of 2–4 input/output examples in a prompt to demonstrate the expected behavior.</p>\n<p><strong>Why few-shot is more effective than textual descriptions:</strong></p>\n<ul><li>A vague instruction like “be more precise” can be interpreted in many ways</li><li>An example unambiguously shows the expected format and decision logic</li><li>The model generalizes the pattern to new cases (it does not just repeat the examples)</li></ul>\n<p><strong>Types of few-shot examples and when to use them:</strong></p>\n<ol><li><strong>Examples for ambiguous scenarios:</strong></li></ol>\n<pre><code>Request: \"My order is broken\"\nAction: Call get_customer -&gt; lookup_order -&gt; check status.\nRationale: “broken” may mean a damaged item; you need order details.\n\nRequest: \"Get me a manager\"\nAction: Immediately call escalate_to_human.\nRationale: The customer explicitly requests a human. Do not attempt to solve autonomously.</code></pre>\n<ol><li><strong>Examples for output formatting:</strong></li></ol>\n<pre><code>Finding example:\n{\n  \"location\": \"src/auth/login.ts:42\",\n  \"issue\": \"SQL injection in the username parameter\",\n  \"severity\": \"critical\",\n  \"suggested_fix\": \"Use a parameterized query\"\n}</code></pre>\n<ol><li><strong>Examples to separate acceptable vs problematic code:</strong></li></ol>\n<pre><code>// Acceptable (do not flag):\nconst items = data.filter(x =&gt; x.active);\n\n// Problem (flag):\nconst items = data.filter(x =&gt; x.active == true); // Use strict equality ===</code></pre>\n<ol><li><strong>Examples for extraction from different document formats:</strong></li></ol>\n<pre><code>Document with inline citations:\n\"As shown in the study (Smith, 2023), the rate is 42%.\"\n-&gt; {\"value\": \"42%\", \"source\": \"Smith, 2023\", \"type\": \"inline_citation\"}\n\nDocument with bibliography references:\n\"The rate is 42%. [1]\"\n-&gt; {\"value\": \"42%\", \"source\": \"reference_1\", \"type\": \"bibliography\"}</code></pre>\n<ol><li><strong>Examples for informal measurements:</strong></li></ol>\n<pre><code>Text: \"about two handfuls of rice\"\n-&gt; {\"amount\": \"~100g\", \"original_text\": \"two handfuls\", \"precision\": \"approximate\"}\n\nText: \"a pinch of salt\"\n-&gt; {\"amount\": \"~1g\", \"original_text\": \"a pinch\", \"precision\": \"approximate\"}</code></pre>\n<p>Few-shot is especially effective for extracting informal and non-standard measurement units that are too diverse for purely rule-based instructions.</p>\n<p><strong>Format normalization rules in prompts:</strong> When using strict JSON schemas for structured output, add normalization rules in the prompt:</p>\n<pre><code>Normalization:\n- Dates: always ISO 8601 (YYYY-MM-DD); \"yesterday\" -&gt; compute an absolute date\n- Currency: numeric amount + currency code; \"five bucks\" -&gt; {\"amount\": 5, \"currency\": \"USD\"}\n- Percentages: decimal fraction; \"half\" -&gt; 0.5</code></pre>\n<p>This prevents semantic errors where the JSON is syntactically valid but values are inconsistent.</p>\n<h3>6.2 Explicit Criteria vs Vague Instructions</h3>\n<p><strong>Bad (vague):</strong></p>\n<pre><code>Check code comments for accuracy.\nBe conservative—report only high-confidence findings.</code></pre>\n<p><strong>Good (explicit criteria):</strong></p>\n<pre><code>Flag a comment as problematic ONLY if:\n1. The comment describes behavior that CONTRADICTS the actual code behavior\n2. The comment references a non-existent function or variable\n3. A TODO/FIXME comment refers to a bug that has already been fixed in code\n\nDo NOT flag:\n- Comments that are merely stylistically outdated\n- Comments with minor wording inaccuracies\n- Missing comments (that is a separate category)</code></pre>\n<p><strong>Define severity criteria with examples:</strong></p>\n<pre><code>CRITICAL: Runtime failure for users\n  Example: NullPointerException while processing a payment\n\nHIGH: Security vulnerability\n  Example: SQL injection, XSS, missing authorization checks\n\nMEDIUM: Logic bug without immediate impact\n  Example: Wrong sorting, off-by-one error\n\nLOW: Code quality\n  Example: Duplication, suboptimal algorithm for small data</code></pre>\n<h3>6.3 Prompt Chaining</h3>\n<p>Prompt chaining breaks a complex task into a sequence of focused steps:</p>\n<pre><code>Step 1: Analyze auth.ts (local issues only)\n       -&gt; Output: list of issues in auth.ts\n\nStep 2: Analyze database.ts (local issues only)\n       -&gt; Output: list of issues in database.ts\n\nStep 3: Integration pass (cross-file dependencies)\n       -&gt; Output: issues at module boundaries</code></pre>\n<p><strong>Why this matters:</strong></p>\n<ul><li>Avoids <strong>attention dilution</strong>—when the model receives too many files at once, it may miss bugs in some files while providing shallow commentary on others</li><li>Ensures consistent analysis quality per file</li><li>Allows separate analysis of cross-file interactions</li></ul>\n<p><strong>When to use prompt chaining vs dynamic decomposition:</strong></p>\n<ul><li><strong>Prompt chaining</strong> — predictable, repeatable tasks (code review, file migrations)</li><li><strong>Dynamic decomposition</strong> — open-ended investigations where subtasks become clear only during execution</li></ul>\n<h3>6.4 The “Interview” Pattern</h3>\n<p>Before implementing a solution, Claude asks clarifying questions:</p>\n<pre><code>Claude: \"Before implementing caching for the API, a few questions:\n1. Which cache invalidation strategy do you prefer—TTL or event-based?\n2. Is stale data acceptable when the cache is unavailable?\n3. Should caching be per-user or global?\n4. What is the expected data volume to cache?\"</code></pre>\n<p><strong>When this is useful:</strong></p>\n<ul><li>Unfamiliar domain (fintech, healthcare, legal systems)</li><li>Tasks with non-obvious implications (cache strategies, failure modes)</li><li>Multiple viable approaches where the best choice depends on context</li></ul>\n<h3>6.5 Validation and Retry-with-Feedback</h3>\n<p>When extracted data fails validation:</p>\n<pre><code>Step 1: Extract data from the document\nStep 2: Validate (Pydantic, JSON Schema, business rules)\nStep 3: If there's an error—retry with context:\n  - The original document\n  - The previous (incorrect) extraction\n  - The specific error: \"Field 'total' = 150, but sum(line_items) = 145. Re-check values.\"</code></pre>\n<p><strong>When retry will be effective:</strong></p>\n<ul><li>Format errors (date in the wrong format)</li><li>Structural errors (a field placed in the wrong location)</li><li>Arithmetic inconsistencies (the model can re-check)</li></ul>\n<p><strong>When retry will NOT help:</strong></p>\n<ul><li>The information is absent from the source document</li><li>The required context is external (the data is in another document not provided)</li></ul>\n<p><strong>Pydantic as a validation tool:</strong> Pydantic is a Python library for schema-based data validation. For the exam, the key points are:</p>\n<ul><li><strong>Structural validation:</strong> types, requiredness, enum constraints checked in code after receiving JSON from Claude</li><li><strong>Semantic validation:</strong> custom validators enforce business logic (sum of items equals total; start_date &lt; end_date)</li><li><strong>Validate–retry loops:</strong> on Pydantic validation failure, construct an error message and re-prompt Claude with the error context</li><li><strong>JSON Schema generation:</strong> Pydantic models can generate JSON Schema for <code>tool_use</code>, providing a single source of truth</li></ul>\n<h3>6.6 Self-correction</h3>\n<p>A pattern for detecting internal contradictions:</p>\n<pre><code class=\"lang-json\">{\n  \"stated_total\": \"$150.00\",\n  \"calculated_total\": \"$145.00\",\n  \"conflict_detected\": true,\n  \"line_items\": [\n    {\"name\": \"Widget A\", \"price\": 75.00},\n    {\"name\": \"Widget B\", \"price\": 70.00}\n  ]\n}</code></pre>\n<p>The model extracts both the stated value and a computed value—if they differ, <code>conflict_detected</code> allows you to handle the discrepancy.</p>"
  },
  {
    "id": "t8",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 7: Message Batches API",
    "html": "<blockquote>Documentation: <a href=\"https://platform.claude.com/docs/en/build-with-claude/message-batches\" target=\"_blank\" rel=\"noopener\">Message Batches</a></blockquote>\n<h3>7.1 Overview</h3>\n<p>The Message Batches API lets you submit batches of requests for asynchronous processing:</p>\n<div class=\"table-wrap\"><table><thead><tr><th>Attribute</th><th>Value</th></tr></thead><tbody><tr><td>Savings</td><td><strong>50%</strong> compared to synchronous calls</td></tr><tr><td>Processing window</td><td>Up to <strong>24 hours</strong> (no latency SLA guarantee)</td></tr><tr><td>Multi-turn tool calling</td><td><strong>Not supported</strong> (one request = one response)</td></tr><tr><td>Correlation</td><td><code>custom_id</code> field to link request and response</td></tr></tbody></table></div>\n<h3>7.2 When to Use Batch API vs Synchronous API</h3>\n<div class=\"table-wrap\"><table><thead><tr><th>Task</th><th>API</th><th>Why</th></tr></thead><tbody><tr><td>Pre-merge PR check</td><td><strong>Synchronous</strong></td><td>The developer is waiting; 24 hours is unacceptable</td></tr><tr><td>Overnight tech-debt report</td><td><strong>Batch</strong></td><td>Result is needed by morning; 50% savings</td></tr><tr><td>Weekly security audit</td><td><strong>Batch</strong></td><td>Not urgent; 50% savings</td></tr><tr><td>Interactive code review</td><td><strong>Synchronous</strong></td><td>Immediate response required</td></tr><tr><td>Processing 10,000 documents</td><td><strong>Batch</strong></td><td>Bulk processing; savings are significant</td></tr></tbody></table></div>\n<h3>7.3 Using <code>custom_id</code></h3>\n<pre><code class=\"lang-json\">{\n  \"custom_id\": \"doc-invoice-2024-001\",\n  \"params\": {\n    \"model\": \"claude-sonnet-4-6\",\n    \"max_tokens\": 1024,\n    \"messages\": [{\"role\": \"user\", \"content\": \"Extract data from: ...\"}]\n  }\n}</code></pre>\n<p><code>custom_id</code> allows you to:</p>\n<ul><li>Link the result to the original document</li><li>On failure, re-submit only the failed documents</li><li>Avoid re-processing successful documents</li></ul>\n<h3>7.4 Handling Failures in Batches</h3>\n<ol><li>Submit a batch of 100 documents</li><li>95 succeed; 5 fail (context limit exceeded)</li><li>Identify failures by <code>custom_id</code></li><li>Modify strategy (e.g., split long documents into chunks)</li><li>Re-submit only the 5 failed documents</li></ol>\n<h3>7.5 SLA Planning</h3>\n<p>If you need a result in 30 hours and the Batch API can take up to 24 hours:</p>\n<ul><li>Submission window: 30 - 24 = <strong>6 hours</strong></li><li>Batches must be submitted no later than 24 hours before the deadline</li><li>For frequent submissions, split into 4-hour windows</li></ul>"
  },
  {
    "id": "t9",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 8: Task Decomposition Strategies",
    "html": "<h3>8.1 Fixed Pipelines (Prompt Chaining)</h3>\n<p>Each step is defined in advance:</p>\n<pre><code>Document -&gt; Metadata extraction -&gt; Data extraction -&gt; Validation -&gt; Enrichment -&gt; Final output</code></pre>\n<p><strong>When to use:</strong></p>\n<ul><li>The task structure is predictable (reviews always follow the same template)</li><li>All steps are known up front</li><li>You need stability and reproducibility</li></ul>\n<h3>8.2 Dynamic Adaptive Decomposition</h3>\n<p>Subtasks are generated based on intermediate results:</p>\n<pre><code>1. \"Add tests for a legacy codebase\"\n2. -&gt; First: map the structure (Glob, Grep)\n3. -&gt; Found: 3 modules with no tests, 2 with partial coverage\n4. -&gt; Prioritize: start with the payments module (high risk)\n5. -&gt; During work: discovered a dependency on an external API\n6. -&gt; Adapt: add a mock for the external API before writing tests</code></pre>\n<p><strong>When to use:</strong></p>\n<ul><li>Open-ended investigative tasks</li><li>When the full scope is unknown up front</li><li>When each step depends on the results of the previous step</li></ul>\n<h3>8.3 Multi-pass Code Review</h3>\n<p>For pull requests with 10+ files:</p>\n<pre><code>Pass 1 (per-file): Analyze auth.ts -&gt; list local issues\nPass 1 (per-file): Analyze database.ts -&gt; list local issues\nPass 1 (per-file): Analyze routes.ts -&gt; list local issues\n...\nPass 2 (integration): Analyze relationships between files\n  -&gt; Cross-file issues: inconsistent types, circular dependencies</code></pre>\n<p><strong>Why a single pass over 14 files is bad:</strong></p>\n<ul><li>Attention dilution: deep analysis for some files, shallow for others</li><li>Inconsistent comments: a pattern is flagged in one file but approved in another</li><li>Missed bugs: obvious errors get skipped due to cognitive overload</li></ul>"
  },
  {
    "id": "t10",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 9: Escalation and Human-in-the-Loop",
    "html": "<h3>9.1 When to Escalate to a Human</h3>\n<p><strong>Escalation triggers (clear rules):</strong></p>\n<div class=\"table-wrap\"><table><thead><tr><th>Situation</th><th>Action</th></tr></thead><tbody><tr><td>The customer explicitly asks “get me a manager”</td><td>Escalate immediately; do not attempt to solve</td></tr><tr><td>Policy does not cover the request</td><td>Escalate (e.g., competitor price matching when policy is silent)</td></tr><tr><td>The agent cannot make progress</td><td>Escalate after a reasonable number of attempts</td></tr><tr><td>Financial operation above a threshold</td><td>Escalate (preferably enforced via a hook, not a prompt)</td></tr><tr><td>Multiple matches when searching for a customer</td><td>Ask for additional identifiers; do not guess</td></tr></tbody></table></div>\n<p><strong>What is NOT a reliable trigger:</strong></p>\n<div class=\"table-wrap\"><table><thead><tr><th>Unreliable method</th><th>Why it fails</th></tr></thead><tbody><tr><td>Sentiment analysis</td><td>Customer mood does not correlate with case complexity</td></tr><tr><td>Model self-rated confidence (1–10)</td><td>The model can be confidently wrong; calibration is poor</td></tr><tr><td>An automatic classifier</td><td>Overengineering; may require training data you don’t have</td></tr></tbody></table></div>\n<h3>9.2 Escalation Patterns</h3>\n<p><strong>Immediate escalation:</strong></p>\n<pre><code>Customer: \"I want to speak to a manager\"\nAgent: [immediately calls escalate_to_human]\nNOT: \"I can help with your issue, let me...\"</code></pre>\n<p><strong>Escalation after an attempt to resolve:</strong></p>\n<pre><code>Customer: \"My refrigerator broke two days after purchase\"\nAgent: [checks the order, offers a warranty replacement]\nIf the customer is not satisfied -&gt; escalate</code></pre>\n<p><strong>Nuanced escalation (acknowledge → resolve → escalate on reiteration):</strong></p>\n<pre><code>Customer: \"This is outrageous, I'm very unhappy with the quality!\"\nAgent: [acknowledges frustration] \"I understand your frustration.\"\n       [offers resolution] \"I can offer a replacement or a refund.\"\nCustomer: \"No, I want to talk to someone!\"\nAgent: [customer insists again -&gt; immediate escalation]</code></pre>\n<p>Key principle: acknowledge emotion first, then propose a concrete solution, and only escalate if the customer reiterates the desire for a human. Do not escalate on the first expression of dissatisfaction (that is not the same as requesting a manager).</p>\n<p><strong>Escalation for a policy gap:</strong></p>\n<pre><code>Customer: \"Competitor X has this item 30% cheaper—give me a discount\"\nPolicy: covers price adjustments only on your own site\nAgent: [escalates — policy does not cover competitor price matching]</code></pre>\n<h3>9.3 Structured Handoff Protocols</h3>\n<p>On escalation, the agent should pass a structured summary to a human:</p>\n<pre><code class=\"lang-json\">{\n  \"customer_id\": \"CUST-12345\",\n  \"customer_name\": \"Ivan Petrov\",\n  \"issue_summary\": \"Refund request for a damaged item\",\n  \"order_id\": \"ORD-67890\",\n  \"root_cause\": \"Item arrived damaged; photos attached\",\n  \"actions_taken\": [\n    \"Verified customer via get_customer\",\n    \"Confirmed order via lookup_order\",\n    \"Offered a standard replacement — customer insists on a refund\"\n  ],\n  \"refund_amount\": \"$89.99\",\n  \"recommended_action\": \"Approve a full refund\",\n  \"escalation_reason\": \"Customer requested to speak with a manager\"\n}</code></pre>\n<p>The human operator does not have access to the full conversation transcript—they only see this summary. Therefore it must be complete and self-contained.</p>\n<h3>9.4 Confidence Calibration and Human Oversight</h3>\n<p>For data extraction systems:</p>\n<ol><li><strong>Field-level confidence scores:</strong> the model outputs a confidence score per extracted field</li><li><strong>Calibration:</strong> use labeled validation sets to tune thresholds</li><li><strong>Routing:</strong></li></ol>\n<ul><li>High confidence + stable accuracy -&gt; automated processing</li><li>Low confidence or ambiguous sources -&gt; human review</li></ul>\n<p><strong>Stratified random sampling:</strong></p>\n<ul><li>Even for high-confidence extractions, regularly audit a sample</li><li>An aggregate 97% accuracy can hide 40% errors for a particular document type</li><li>Analyze accuracy by document type and by field, not only overall</li></ul>"
  },
  {
    "id": "t11",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 10: Error Handling in Multi-agent Systems",
    "html": "<h3>10.1 Error Categories</h3>\n<div class=\"table-wrap\"><table><thead><tr><th>Category</th><th>Examples</th><th>Retryable</th><th>Agent action</th></tr></thead><tbody><tr><td><strong>Transient</strong></td><td>Timeout, 503, network failure</td><td>Yes</td><td>Retry with exponential backoff</td></tr><tr><td><strong>Validation</strong></td><td>Invalid input format, missing required field</td><td>No (fix input)</td><td>Modify request and retry</td></tr><tr><td><strong>Business</strong></td><td>Policy violation, threshold exceeded</td><td>No</td><td>Explain to the user; propose an alternative</td></tr><tr><td><strong>Permission</strong></td><td>Access denied</td><td>No</td><td>Escalate</td></tr></tbody></table></div>\n<h3>10.2 Error-handling Anti-patterns</h3>\n<div class=\"table-wrap\"><table><thead><tr><th>Anti-pattern</th><th>Problem</th><th>Correct approach</th></tr></thead><tbody><tr><td>Generic status “search unavailable”</td><td>The coordinator can’t decide how to recover</td><td>Return error type, query, partial results, alternatives</td></tr><tr><td>Silent suppression (empty result = success)</td><td>Coordinator thinks there were no matches, but it was a failure</td><td>Distinguish “no results” from “search failure”</td></tr><tr><td>Aborting the whole workflow on one failure</td><td>You lose all partial results</td><td>Continue with partial results; annotate gaps</td></tr><tr><td>Infinite retries inside a subagent</td><td>Latency and wasted resources</td><td>Local recovery (1–2 retries), then propagate to coordinator</td></tr></tbody></table></div>\n<h3>10.3 A Structured Subagent Error</h3>\n<pre><code class=\"lang-json\">{\n  \"status\": \"partial_failure\",\n  \"failure_type\": \"timeout\",\n  \"attempted_query\": \"AI impact on music industry 2024\",\n  \"partial_results\": [\n    {\"title\": \"AI Music Generation Report\", \"url\": \"...\", \"relevance\": 0.8}\n  ],\n  \"alternative_approaches\": [\n    \"Try a narrower query: 'AI music composition tools'\",\n    \"Use an alternative data source\"\n  ],\n  \"coverage_impact\": \"Not covered: AI impact on music production\"\n}</code></pre>\n<p>This provides the coordinator with the information needed to decide:</p>\n<ul><li>Retry with a modified query?</li><li>Use partial results?</li><li>Delegate to a different subagent?</li><li>Continue without this section and annotate the gap?</li></ul>\n<h3>10.4 Coverage Annotations in the Final Synthesis</h3>\n<pre><code class=\"lang-markdown\">## Report: AI Impact on Creative Industries\n\n### Visual Art (FULL COVERAGE)\n[research results]\n\n### Music (PARTIAL COVERAGE — search agent timeout)\n[partial results]\n⚠️ Note: coverage for this section is limited due to a timeout in the search agent.\n\n### Literature (FULL COVERAGE)\n[research results]</code></pre>"
  },
  {
    "id": "t12",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 11: Context Management in Production Systems",
    "html": "<h3>11.1 Extract Facts into a Separate Block</h3>\n<p>Instead of relying on conversation history (which degrades during summarization), extract key facts into a structured block:</p>\n<pre><code>=== CASE FACTS (updated whenever a new fact appears) ===\nCustomer ID: CUST-12345\nOrder ID: ORD-67890\nOrder Date: 2025-01-15\nOrder Amount: $89.99\nIssue: Damaged item on delivery\nCustomer Request: Full refund\nStatus: Pending manager approval\n===</code></pre>\n<p>Include this block in every prompt, regardless of how history is summarized.</p>\n<h3>11.2 Trimming Tool Results</h3>\n<p>If <code>lookup_order</code> returns 40+ fields but you only need 5 for the current task:</p>\n<pre><code class=\"lang-python\"># PostToolUse hook: keep only relevant fields\n@hook(\"PostToolUse\", tool=\"lookup_order\")\ndef trim_order_fields(result):\n    return {\n        \"order_id\": result[\"order_id\"],\n        \"status\": result[\"status\"],\n        \"total\": result[\"total\"],\n        \"items\": result[\"items\"],\n        \"return_eligible\": result[\"return_eligible\"]\n    }</code></pre>\n<p>This conserves context and reduces noise.</p>\n<h3>11.3 Position-aware Input</h3>\n<p>Place critical information with the lost-in-the-middle effect in mind:</p>\n<pre><code>[KEY FINDINGS — at the top]\nFound 3 critical vulnerabilities...\n\n[DETAILED RESULTS — middle]\n=== File auth.ts ===\n...\n=== File database.ts ===\n...\n\n[ACTION ITEMS — at the end]\nPriority: fix auth.ts vulnerabilities before merge.</code></pre>\n<h3>11.4 Scratchpad Files</h3>\n<p>In long investigations, the agent can write key findings to a scratchpad file:</p>\n<pre><code># investigation-scratchpad.md\n## Key findings\n- PaymentProcessor in src/payments/processor.ts inherits from BaseProcessor\n- refund() is called from 3 places: OrderController, AdminPanel, CronJob\n- External PaymentGateway API has a rate limit of 100 req/min\n- Migration #47 added refund_reason (NOT NULL) — 2024-12-01</code></pre>\n<p>When context degrades (or in a new session), the agent can consult the scratchpad instead of re-running discovery.</p>\n<h3>11.5 Delegating to Subagents to Protect Context</h3>\n<pre><code>Main agent: \"Investigate dependencies of the payments module\"\n  -&gt; Subagent (Explore): reads 15 files, traces imports\n  -&gt; Returns: \"Payments depends on AuthService, OrderModel, and the external PaymentGateway API\"\n\nMain agent: keeps one line in context instead of 15 files</code></pre>\n<p><strong>Separate context layer:</strong> In multi-agent systems, each subagent operates within a limited context budget—it receives only the information required for its task. The coordinator acts as a separate context layer: it aggregates subagent outputs, stores global state, and allocates context. This prevents “context leakage,” where one agent consumes the window with information irrelevant to others.</p>\n<p><strong>Constrained context budgets for subagents:</strong></p>\n<ul><li>Send minimal context: a specific task + necessary data</li><li>Instruct the subagent to return structured results, not raw data dumps</li><li>Use <code>allowedTools</code> to limit the subagent’s toolset—fewer tools means fewer distractions and lower context cost</li></ul>\n<h3>11.6 Structured State Persistence (for crash recovery)</h3>\n<p>Each agent exports its state to a known location:</p>\n<pre><code class=\"lang-json\">// agent-state/web-search-agent.json\n{\n  \"status\": \"completed\",\n  \"queries_executed\": [\"AI music 2024\", \"AI music composition\"],\n  \"results_count\": 12,\n  \"key_findings\": [...],\n  \"coverage\": [\"music composition\", \"music production\"],\n  \"gaps\": [\"music distribution\", \"music licensing\"]\n}</code></pre>\n<p>The coordinator loads a manifest on resume:</p>\n<pre><code class=\"lang-json\">// agent-state/manifest.json\n{\n  \"web-search\": \"completed\",\n  \"doc-analysis\": \"in_progress\",\n  \"synthesis\": \"not_started\"\n}</code></pre>"
  },
  {
    "id": "t13",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 12: Preserving Provenance",
    "html": "<h3>12.1 The Attribution Loss Problem</h3>\n<p>When summarizing results from multiple sources, the “claim → source” link can be lost:</p>\n<pre><code>Bad: \"The AI music market is estimated at $3.2B.\" (No source, no year.)\n\nGood:\n{\n  \"claim\": \"The AI music market is estimated at $3.2B.\",\n  \"source_url\": \"https://example.com/report\",\n  \"source_name\": \"Global AI Music Report 2024\",\n  \"publication_date\": \"2024-06-15\",\n  \"confidence\": 0.9\n}</code></pre>\n<h3>12.2 Handling Conflicting Data</h3>\n<p>When two sources provide different values:</p>\n<pre><code class=\"lang-json\">{\n  \"claim\": \"Share of AI-generated music on streaming platforms\",\n  \"values\": [\n    {\n      \"value\": \"12%\",\n      \"source\": \"Spotify Annual Report 2024\",\n      \"date\": \"2024-03\",\n      \"methodology\": \"Automated classification\"\n    },\n    {\n      \"value\": \"8%\",\n      \"source\": \"Music Industry Association Survey\",\n      \"date\": \"2024-07\",\n      \"methodology\": \"Survey of 500 labels\"\n    }\n  ],\n  \"conflict_detected\": true,\n  \"possible_explanation\": \"Difference in methodology and time period\"\n}</code></pre>\n<p>Do not arbitrarily choose one value. Preserve both with attribution and let the coordinator decide.</p>\n<h3>12.3 Include Dates for Correct Interpretation</h3>\n<p>Without dates, temporal differences can be misinterpreted as contradictions:</p>\n<pre><code>Bad: \"Source A says 10%, source B says 15%. Contradiction.\"\nGood: \"Source A (2023) says 10%, source B (2024) says 15%. Likely +5% growth over a year.\"</code></pre>\n<h3>12.4 Render by Content Type</h3>\n<p>Don’t force everything into one format:</p>\n<ul><li>Financial data -&gt; tables</li><li>News and analysis -&gt; prose</li><li>Technical findings -&gt; structured lists</li><li>Time series -&gt; chronological ordering</li></ul>"
  },
  {
    "id": "t14",
    "part": "Part I — Theory Foundations",
    "title": "Chapter 13: Claude Code Built-in Tools",
    "html": "<h3>13.1 Tool Selection Reference</h3>\n<div class=\"table-wrap\"><table><thead><tr><th>Task</th><th>Tool</th><th>Example</th></tr></thead><tbody><tr><td>Find files by name/pattern</td><td><strong>Glob</strong></td><td><code>**/*.test.tsx</code>, <code>src/components/**/*.ts</code></td></tr><tr><td>Search within files</td><td><strong>Grep</strong></td><td>Function name, error message, import</td></tr><tr><td>Read a file in full</td><td><strong>Read</strong></td><td>Load a file for analysis</td></tr><tr><td>Write a new file</td><td><strong>Write</strong></td><td>Create a file from scratch</td></tr><tr><td>Edit an existing file precisely</td><td><strong>Edit</strong></td><td>Replace a specific snippet via unique text match</td></tr><tr><td>Run a shell command</td><td><strong>Bash</strong></td><td>git, npm, run tests, build</td></tr></tbody></table></div>\n<h3>13.2 Incremental Investigation Strategy</h3>\n<p>Do not read all files at once. Build understanding incrementally:</p>\n<pre><code>1. Grep: find entry points (function definition, export)\n2. Read: read the found files\n3. Grep: find usages (import, calls)\n4. Read: read consumer files\n5. Repeat until you have a complete picture</code></pre>\n<h3>13.3 Fallback: Read + Write Instead of Edit</h3>\n<p>When Edit fails due to a non-unique text match:</p>\n<ol><li>Read — load the full file content</li><li>Modify the content programmatically</li><li>Write — write the updated version</li></ol>"
  },
  {
    "id": "t15",
    "part": "Part II — Exam Domain Notes",
    "title": "Domain 1: Agent Architecture and Orchestration (27%)",
    "html": "<h3>1.1 Designing Agentic Loops for Autonomous Task Execution</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Agent loop lifecycle: send a Claude request, check <code>stop_reason</code> (<code>\"tool_use\"</code> vs <code>\"end_turn\"</code>), execute tools, return results for the next iteration</li><li>Tool results are appended to the conversation history so the model can decide the next action</li><li>Model-driven decision making (Claude chooses the next tool) vs hard-coded decision trees</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Flow control: continue the loop when <code>stop_reason = \"tool_use\"</code> and stop on <code>\"end_turn\"</code></li><li>Appending tool results to context between iterations</li><li>Anti-patterns to avoid: parsing assistant text for completion, using arbitrary iteration limits as the primary stopping mechanism</li></ul>\n<h3>1.2 Orchestrating Multi-agent Systems (Coordinator–Subagent)</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Hub-and-spoke architecture: the coordinator owns all inter-agent communication, error handling, and routing</li><li>Subagents operate with isolated context—they do not automatically inherit the coordinator’s history</li><li>Coordinator responsibilities: task decomposition, delegation, result aggregation, dynamic selection of subagents</li><li>Risk of overly narrow decomposition by the coordinator</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Split research coverage among subagents to minimize duplication</li><li>Implement iterative refinement loops (coordinator evaluates synthesis and re-routes tasks)</li><li>Route all communication through the coordinator for observability</li></ul>\n<h3>1.3 Configuring Subagent Calls, Context Passing, and Spawning</h3>\n<h4>Key knowledge:</h4>\n<ul><li><code>Task</code> tool spawns subagents; the coordinator’s <code>allowedTools</code> must include <code>\"Task\"</code></li><li>Subagent context must be explicitly included in the prompt; subagents do not inherit parent context</li><li><code>AgentDefinition</code> configuration: descriptions, system prompts, tool constraints</li><li>Session management via <code>fork_session</code> for exploring alternatives</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Include full outputs from prior agents in the subagent prompt</li><li>Use structured formats to separate data from metadata when passing context</li><li>Spawn parallel subagents via multiple <code>Task</code> calls in a single coordinator turn</li><li>Write coordinator prompts in terms of goals and quality criteria rather than step-by-step instructions</li></ul>\n<h3>1.4 Implementing Multi-step Workflows with Enforcement and Handoff Patterns</h3>\n<h4>Key knowledge:</h4>\n<ul><li>The difference between <strong>programmatic enforcement</strong> (hooks, preconditions) and <strong>prompt guidance</strong> for ordering a workflow</li><li>When you need deterministic guarantees (e.g., identity verification before financial operations), prompts alone are insufficient</li><li>Structured handoff protocols during escalation (customer ID, reason, recommended action)</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Programmatic preconditions that block downstream calls until prior steps are complete (e.g., block <code>process_refund</code> until <code>get_customer</code> returns a verified ID)</li><li>Decompose multi-aspect customer requests into separate items</li><li>Produce structured summaries when escalating to a human</li></ul>\n<h3>1.5 Agent SDK Hooks for Intercepting Tool Calls and Normalizing Data</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Hook patterns (e.g., <code>PostToolUse</code>) to intercept tool results before the model consumes them</li><li>Hooks that intercept outgoing calls to enforce compliance rules (e.g., block refunds above a threshold)</li><li>Hooks provide <strong>deterministic guarantees</strong> vs prompt instructions that provide <strong>probabilistic compliance</strong></li></ul>\n<h4>Key skills:</h4>\n<ul><li><code>PostToolUse</code> hooks for normalizing data formats (Unix timestamps, ISO 8601, numeric status codes)</li><li>Interception hooks to block policy-violating actions with redirection to escalation</li><li>Choose hooks over prompts when business rules require guaranteed compliance</li></ul>\n<h3>1.6 Task Decomposition Strategies for Complex Workflows</h3>\n<h4>Key knowledge:</h4>\n<ul><li><strong>Fixed pipelines</strong> (prompt chaining) vs <strong>dynamic adaptive decomposition</strong> based on intermediate results</li><li>Prompt chaining: sequential steps (analyze each file separately, then run an integration pass)</li><li>Adaptive investigation plans that generate subtasks based on what was discovered</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Use prompt chaining for predictable multi-aspect reviews; use dynamic decomposition for open-ended investigations</li><li>Split large code reviews into per-file analysis plus a separate cross-file integration pass</li><li>Decompose open-ended tasks: map structure first, then build a prioritized plan</li></ul>\n<h3>1.7 Session State, Resuming, and Forking</h3>\n<h4>Key knowledge:</h4>\n<ul><li><code>--resume &lt;session-name&gt;</code> to continue named sessions</li><li><code>fork_session</code> to create independent investigation branches from shared context</li><li>The importance of informing the agent about file changes when resuming sessions</li><li>A new session with a structured summary can be more reliable than resuming with stale results</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Use <code>--resume</code> to continue named investigation sessions</li><li>Use <code>fork_session</code> to compare approaches in parallel</li><li>Choose between resuming (context still current) vs starting a new session (results stale)</li></ul>"
  },
  {
    "id": "t16",
    "part": "Part II — Exam Domain Notes",
    "title": "Domain 2: Tool Design and MCP Integration (18%)",
    "html": "<h3>2.1 Designing Tool Interfaces with Clear Descriptions</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Tool descriptions are the <strong>primary mechanism</strong> an LLM uses to select tools; minimal descriptions lead to unreliable selection</li><li>The importance of including input formats, example queries, edge cases, and applicability boundaries</li><li>Ambiguous or overlapping descriptions cause misrouting</li><li>System prompt wording can create unintended associations with tools</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Write descriptions that clearly distinguish each tool from similar alternatives</li><li>Rename tools to eliminate functional overlap (e.g., <code>analyze_content</code> -&gt; <code>extract_web_results</code>)</li><li>Split general-purpose tools into specialized ones with clear input/output contracts</li></ul>\n<h3>2.2 Implementing Structured Error Responses for MCP Tools</h3>\n<h4>Key knowledge:</h4>\n<ul><li>The <code>isError</code> flag in MCP tool responses</li><li>The difference between <strong>transient errors</strong> (timeouts), <strong>validation errors</strong> (bad input), <strong>business errors</strong> (policy violations), and <strong>access/permission errors</strong></li><li>Generic errors (\"Operation failed\") prevent correct recovery decisions</li><li>The difference between retryable and non-retryable errors</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Return structured metadata such as <code>errorCategory</code> (transient/validation/permission), <code>isRetryable</code>, and a human-readable message</li><li>Use <code>retryable: false</code> for business-rule violations with clear user-facing explanations</li><li>Do local recovery inside subagents for transient failures; propagate only errors they cannot resolve</li><li>Distinguish access failures (retry decision) from valid empty results (no matches)</li></ul>\n<h3>2.3 Allocating Tools Across Agents and Configuring <code>tool_choice</code></h3>\n<h4>Key knowledge:</h4>\n<ul><li>Too many tools per agent (e.g., 18 instead of 4–5) <strong>reduces</strong> tool selection reliability</li><li>Agents with tools outside their specialization tend to misuse them</li><li>Scoped tool access: only role-relevant tools plus a limited set of cross-role utilities</li><li><code>tool_choice</code>: <code>\"auto\"</code>, <code>\"any\"</code>, and forced tool selection (<code>{\"type\": \"tool\", \"name\": \"...\"}</code>)</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Restrict each subagent’s toolset to what is relevant for its role</li><li>Replace general tools with constrained alternatives (e.g., <code>fetch_url</code> -&gt; <code>load_document</code>)</li><li>Use <code>tool_choice: \"any\"</code> to guarantee a tool call instead of a text answer</li><li>Force a specific tool to ensure execution order</li></ul>\n<h3>2.4 Integrating MCP Servers into Claude Code and Agent Workflows</h3>\n<h4>Key knowledge:</h4>\n<ul><li>MCP server scope: project (<code>.mcp.json</code>) for teams vs user (<code>~/.claude.json</code>) for experiments</li><li>Environment variable substitution in <code>.mcp.json</code> (e.g., <code>${GITHUB_TOKEN}</code>) for secret management</li><li>Tools from all connected MCP servers are discovered on connection and are available simultaneously</li><li>MCP resources as “content catalogs” (task summaries, database schemas) to reduce exploratory tool calls</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Configure shared MCP servers in project <code>.mcp.json</code> with env-var-based tokens</li><li>Keep personal/experimental servers in <code>~/.claude.json</code></li><li>Prefer community MCP servers over custom servers for standard integrations</li></ul>\n<h3>2.5 Selecting and Applying Built-in Tools (Read, Write, Edit, Bash, Grep, Glob)</h3>\n<h4>Key knowledge:</h4>\n<ul><li><strong>Grep</strong>: search within file contents (function names, error messages, imports)</li><li><strong>Glob</strong>: find files by name/extension patterns</li><li><strong>Read/Write</strong>: full-file operations; <strong>Edit</strong>: precise changes via unique text matches</li><li>If Edit fails due to non-unique matches, fall back to Read + Write</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Use Grep for content search and Glob for file discovery by patterns</li><li>Build understanding incrementally: Grep entry points, then Read to trace flows</li><li>Trace function usage through wrapper modules</li></ul>"
  },
  {
    "id": "t17",
    "part": "Part II — Exam Domain Notes",
    "title": "Domain 3: Claude Code Configuration and Workflows (20%)",
    "html": "<h3>3.1 Configuring CLAUDE.md with Hierarchy, Scope, and Modular Organization</h3>\n<h4>Key knowledge:</h4>\n<ul><li>CLAUDE.md hierarchy: user (<code>~/.claude/CLAUDE.md</code>), project (<code>.claude/CLAUDE.md</code> or root <code>CLAUDE.md</code>), and directory-level (CLAUDE.md in subdirectories)</li><li>User-level settings apply only to one user and are not shared via VCS</li><li><code>@path</code> syntax for referencing external files (e.g., <code>@./standards/coding-style.md</code>) to modularize CLAUDE.md</li><li>The <code>.claude/rules/</code> directory for topic-focused rule files instead of a monolithic CLAUDE.md</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Diagnose hierarchy issues (a new team member misses instructions because they are user-level instead of project-level)</li><li>Use <code>@path</code> (e.g., <code>@./standards/testing.md</code>) to selectively include standards in each package’s CLAUDE.md</li><li>Split large CLAUDE.md into multiple <code>.claude/rules/</code> files (testing.md, api-conventions.md, deployment.md)</li></ul>\n<h3>3.2 Creating and Configuring Custom Slash Commands and Skills</h3>\n<h4>Key knowledge:</h4>\n<ul><li><strong>Project commands</strong> in <code>.claude/commands/</code> (shared via VCS) vs <strong>user commands</strong> in <code>~/.claude/commands/</code></li><li>Skills in <code>.claude/skills/</code> with <code>SKILL.md</code> frontmatter: <code>context: fork</code>, <code>allowed-tools</code>, <code>argument-hint</code></li><li><code>context: fork</code> runs the skill in an isolated subagent context so it does not pollute the main session</li><li>Personal skill variants can live in <code>~/.claude/skills/</code> under different names</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Store project slash commands in <code>.claude/commands/</code> so the whole team gets them</li><li>Use <code>context: fork</code> to isolate skills with verbose output</li><li>Use <code>allowed-tools</code> to restrict what tools a skill can use</li><li>Use <code>argument-hint</code> to prompt developers for required parameters</li></ul>\n<h3>3.3 Using Path-specific Rules for Conditional Convention Loading</h3>\n<h4>Key knowledge:</h4>\n<ul><li><code>.claude/rules/</code> files can include YAML frontmatter <code>paths</code> to activate rules based on glob patterns</li><li>Path-scoped rules load <strong>only</strong> when editing matching files, saving context and tokens</li><li>Glob-based path rules can be preferable to directory-level CLAUDE.md when conventions apply across many directories (e.g., tests)</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Create <code>.claude/rules/</code> files with <code>paths: [\"terraform/**/*\"]</code> to load only when working on matching files</li><li>Use glob patterns (<code>**/*.test.tsx</code>) to apply conventions by file type regardless of location</li><li>Prefer path-specific rules over directory-level CLAUDE.md when conventions span the codebase</li></ul>\n<h3>3.4 Deciding When to Use Planning Mode vs Direct Execution</h3>\n<h4>Key knowledge:</h4>\n<ul><li><strong>Planning mode</strong>: for complex tasks with large changes, multiple viable approaches, and architectural decisions</li><li><strong>Direct execution</strong>: for simple, well-understood changes (e.g., adding a single validation)</li><li>Planning mode enables safe exploration of the codebase before making changes</li><li>Explore subagent isolates verbose discovery output</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Use planning mode for tasks with architectural consequences (microservices, migrations touching 45+ files)</li><li>Use direct execution for fixes with a clear stack trace and a single file</li><li>Use Explore subagent to prevent context-window exhaustion in multi-phase tasks</li><li>Combine approaches: plan for discovery, then execute for implementation</li></ul>\n<h3>3.5 Iterative Refinement for Progressive Improvement</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Concrete input/output examples are the most effective way to communicate expectations</li><li><strong>Test-driven iteration</strong>: write tests first, then iterate based on failures</li><li>The “interview” pattern: Claude asks questions to surface non-obvious design considerations</li><li>When to provide all issues in one message (interdependent) vs sequentially (independent)</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Provide 2–3 concrete input/output examples to clarify transformation requirements</li><li>Build test sets with expected behavior, edge cases, and performance requirements before implementation</li><li>Use the interview pattern to surface design aspects (cache invalidation, failure modes)</li><li>Provide concrete test cases with sample inputs and expected outputs for edge cases</li></ul>\n<h3>3.6 Integrating Claude Code into CI/CD Pipelines</h3>\n<h4>Key knowledge:</h4>\n<ul><li>The <code>-p</code> (or <code>--print</code>) flag for non-interactive mode in automated pipelines</li><li><code>--output-format json</code> and <code>--json-schema</code> for structured output in CI</li><li>CLAUDE.md provides project context (testing standards, review criteria) for CI-triggered Claude Code</li><li><strong>Session context isolation</strong>: the same session that generated code is less effective at reviewing it than an independent instance</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Run Claude Code in CI with <code>-p</code> to avoid hanging on interactive input</li><li>Use <code>--output-format json</code> + <code>--json-schema</code> for structured results (e.g., inline PR comments)</li><li>Include prior review results when re-running after new commits (report only new/unfixed issues)</li><li>Document testing standards and available fixtures in CLAUDE.md to improve test generation quality</li><li>Include existing test files in context when generating new tests to avoid duplication and keep style consistent</li></ul>"
  },
  {
    "id": "t18",
    "part": "Part II — Exam Domain Notes",
    "title": "Domain 4: Prompt Engineering and Structured Output (20%)",
    "html": "<h3>4.1 Designing Prompts with Explicit Criteria to Improve Accuracy</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Explicit criteria are more effective than vague instructions (e.g., “flag comments only when they contradict code” vs “check comment accuracy”)</li><li>Generic guidance like “be more conservative” works worse than concrete categorical criteria</li><li>The effect of false positives on developer trust: high false-positive rates in some categories undermine trust in accurate categories</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Define review criteria: what to report (bugs, security) vs what to ignore (minor style)</li><li>Temporarily disable categories with high false-positive rates</li><li>Define explicit severity criteria with code examples for each level</li></ul>\n<h3>4.2 Using Few-shot Prompting to Improve Output Consistency</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Few-shot examples are the most effective method for producing consistently formatted, actionable output</li><li>Few-shot can demonstrate handling of ambiguous cases (tool selection, gaps in test coverage)</li><li>Few-shot helps the model generalize to new patterns rather than just repeating defaults</li><li>Few-shot can reduce hallucinations in extraction tasks</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Provide 2–4 targeted examples for ambiguous scenarios with rationale</li><li>Include few-shot examples that demonstrate the output format (location, issue, severity, suggested fix)</li><li>Provide examples that distinguish acceptable code patterns from real issues</li><li>Provide examples of correct extraction from documents with different structures</li></ul>\n<h3>4.3 Enforcing Structured Output with <code>tool_use</code> and JSON Schemas</h3>\n<h4>Key knowledge:</h4>\n<ul><li><code>tool_use</code> with JSON Schemas is the most reliable way to guarantee schema-conformant output and eliminate JSON syntax errors</li><li>With <code>tool_choice: \"auto\"</code> the model can return text; with <code>\"any\"</code> it must call a tool; forced selection chooses a specific tool</li><li>Strict JSON Schemas eliminate syntax errors but do not prevent semantic errors (totals don’t add up; values in wrong fields)</li><li>Schema design: required vs optional fields; enums with “other” plus a detail string for extensibility</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Define extraction tools with JSON Schemas and parse data from <code>tool_use</code> results</li><li>Use <code>tool_choice: \"any\"</code> to guarantee structured output when multiple schemas exist</li><li>Force a specific tool call: <code>tool_choice: {\"type\": \"tool\", \"name\": \"extract_metadata\"}</code></li><li>Make fields optional/nullable when the source may not contain information to avoid fabricating values</li><li>Use enum values like <code>\"unclear\"</code> and <code>\"other\"</code> plus detail fields for extensible categorization</li></ul>\n<h3>4.4 Implementing Validation, Retries, and Feedback Loops for Extraction Quality</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Retry-with-error-feedback: include concrete validation errors in the retry prompt to guide corrections</li><li>Retries are ineffective when the information is simply absent from the source</li><li>Feedback loop design: track the pattern that triggered a finding (<code>detected_pattern</code>)</li><li>Semantic errors (totals don’t reconcile) vs syntax errors (addressed by <code>tool_use</code>)</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Follow-up prompts with the original document, an incorrect extraction, and specific validation errors</li><li>Identify when retry will be ineffective (the required info is only in an external document)</li><li>Include <code>detected_pattern</code> fields in findings to analyze false positives</li><li>Design self-correction by extracting both <code>calculated_total</code> and <code>stated_total</code> to detect discrepancies</li></ul>\n<h3>4.5 Designing Efficient Batch Processing Strategies</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Message Batches API: 50% savings, up to 24-hour processing window, no latency SLA guarantees</li><li>Batch processing is suitable for non-blocking tasks (overnight reports, audits) and not suitable for blocking tasks (pre-merge checks)</li><li>Batch API does not support multi-turn tool calling within a single request</li><li><code>custom_id</code> fields correlate request/response within batches</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Use synchronous API for blocking checks; use Batch API for overnight/weekly workloads</li><li>Plan batch submission cadence based on SLA needs (e.g., 4-hour windows for a 30-hour guarantee with 24-hour processing)</li><li>Handle failures by re-submitting only failed documents (identified by <code>custom_id</code>)</li><li>Iterate on prompts using a sample before running large-scale processing</li></ul>\n<h3>4.6 Designing Multi-instance and Multi-pass Review Architectures</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Self-review limitations: the model retains its reasoning context and is less likely to challenge its own decisions</li><li>Independent review instances (without generation context) are better at finding subtle issues</li><li>Multi-pass review: per-file local analysis plus a cross-file integration pass to avoid attention dilution</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Use a second independent Claude instance to review changes without generation context</li><li>Split multi-file reviews into per-file passes plus integration passes for cross-file dataflow analysis</li><li>Use verification passes with self-rated confidence to route reviews in a calibrated way</li></ul>"
  },
  {
    "id": "t19",
    "part": "Part II — Exam Domain Notes",
    "title": "Domain 5: Context Management and Reliability (15%)",
    "html": "<h3>5.1 Managing Conversation Context to Preserve Critical Information</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Risks of progressive summarization: numeric values, percentages, and dates get condensed into vague summaries</li><li>Lost-in-the-middle effect: models reliably process the start and end of long inputs, but may miss findings from the middle</li><li>Tool outputs can accumulate in context disproportionately to relevance (40+ fields when 5 are needed)</li><li>The importance of sending the full conversation history in subsequent API requests</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Extract transactional facts into a persistent “case facts” block outside the summarized history</li><li>Trim verbose tool outputs down to relevant fields</li><li>Place key findings at the beginning of aggregated data with explicit section headings</li><li>Require subagents to include metadata (dates, sources) in structured outputs</li></ul>\n<h3>5.2 Designing Effective Escalation Patterns and Resolving Ambiguity</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Suitable escalation triggers: explicit request for a human, policy gaps/exceptions, inability to make progress</li><li>Immediate escalation (explicit request) vs attempt-to-resolve (within agent scope)</li><li>Sentiment analysis and model confidence self-ratings are unreliable proxies for case complexity</li><li>Multiple customer matches require asking for additional identifiers, not heuristic guessing</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Explicit escalation criteria with few-shot examples in the system prompt</li><li>Execute explicit requests for a human immediately without additional investigation</li><li>Escalate when policy is ambiguous or silent for a specific request</li><li>Ask for additional identifiers when tool results contain multiple matches</li></ul>\n<h3>5.3 Implementing Error Propagation Strategies in Multi-agent Systems</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Structured error context (failure type, query, partial results, alternatives) enables smarter coordinator recovery</li><li>Distinguish access failures (timeouts require a retry decision) from valid empty results (no matches)</li><li>Generic error statuses (“search unavailable”) hide valuable context from the coordinator</li><li>Silent suppression or aborting the whole workflow on a single failure are both anti-patterns</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Return structured error context: failure type, what was attempted, partial results, possible alternatives</li><li>Distinguish access failures from valid empty results</li><li>Perform local recovery in subagents for transient failures; propagate only non-recoverable errors with partial results</li><li>Annotate coverage in synthesis: what is well-supported vs where gaps remain</li></ul>\n<h3>5.4 Managing Context Efficiently When Investigating Large Codebases</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Context degradation in long sessions: the model starts producing unstable answers and referring to “typical patterns” instead of specific classes</li><li>Scratchpad files preserve key findings across context boundaries</li><li>Delegating to subagents isolates verbose discovery output</li><li>Structured state persistence enables crash recovery</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Spawn subagents for specific questions while keeping high-level coordination in the main agent</li><li>Use scratchpad files to store key findings and reference them later</li><li>Summarize key findings before spawning next-phase subagents</li><li>Use <code>/compact</code> to reduce context usage during long investigations</li></ul>\n<h3>5.5 Designing Workflows with Human Oversight and Confidence Calibration</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Aggregate metrics (e.g., 97% overall accuracy) can mask poor performance on specific document types or fields</li><li>Stratified random sampling measures error rates in high-confidence extractions</li><li>Field-level confidence calibration using labeled validation sets</li><li>Validate accuracy by document type and field segment before automating</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Implement stratified random sampling to detect new error patterns</li><li>Analyze accuracy by document type and field to validate stable performance</li><li>Output field-level confidence scores and calibrate review thresholds using labeled data</li><li>Route low-confidence or ambiguous-source extractions to human review</li></ul>\n<h3>5.6 Preserving Provenance and Handling Uncertainty in Multi-source Synthesis</h3>\n<h4>Key knowledge:</h4>\n<ul><li>Attribution is lost during summarization without preserving “claim → source” mappings</li><li>Structured mappings must be preserved during aggregation</li><li>Handle conflicting statistics by annotating conflicts with attribution rather than arbitrarily choosing one value</li><li>Include publication/collection dates to avoid misreading temporal differences as contradictions</li></ul>\n<h4>Key skills:</h4>\n<ul><li>Require subagents to output “claim → source” mappings (URL, document name, quotes)</li><li>Structure reports to separate stable findings from disputed ones</li><li>Preserve conflicting values with annotations and pass them to the coordinator for reconciliation</li><li>Include publication dates for correct temporal interpretation</li><li>Render content by type: financial data as tables, news as prose, technical findings as structured lists</li></ul>"
  }
];
